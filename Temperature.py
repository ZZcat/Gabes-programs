temp = input("What is the temperature?: ")
if temp > 100: # this says if temp is more then 100 to the next tab lines Note if this is ran the following elif and elses will NOT run
        print "You should probably stay inside today"
elif temp > 70: # this is almost exactly like the the if but it will only run if the if is not true
        print "Wear shorts"
elif temp < 70: 
        print "Wear long pants"
